en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            password:
              too_short: 'must be at least 6 characters'
            password_confirmation:
              confirmation: 'passwords must match'
